Taskflow-API

### Tech stack

- Node.js for Backend
- Postgress as database

### How to run

At root level run the command `npm run dev` this will spin up the backend server which will conenct to db as well.
